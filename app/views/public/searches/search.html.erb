<h2>Results index</h2>

<table class="table table-hover table-inverse">
  <!--検索対象モデルがUserの時 -->
  <% @range = "User" %>
      <% @users.each do |user| %>
        <tr>
          <td><%= link_to user.name, user %></td>
        </tr>
      <% end %>
</table>